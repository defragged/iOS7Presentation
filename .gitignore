# Xcode
.DS_Store
*/build/*
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata
profile
*.moved-aside
DerivedData
.idea/
*.hmap

# Cocoapods
# For now, not checking in any derived Cocoapods stuff
# This requires all collaborators to handle cocoapods stuff themselves.
# If this gets too complicated, we can remove this, and check them into
# source control later.
Pods
Podfile.lock
Acknowledgements.plist

# Appledoc
appledoc
